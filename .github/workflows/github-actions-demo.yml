name: GitHub Actions Demo
on:
  workflow_dispatch:
    inputs:
      release:
        description: 'release version'     
        required: true
jobs:
#   Explore-GitHub-Actions:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#       - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#       - run: echo "🔎 Branch ${{ github.ref }} repository ${{ github.repository }}."
#       - name: Check out repository code
#         uses: actions/checkout@v2
#       - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#       - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#       - name: List files in the repository
#         run: ls -l ${{ github.workspace }}
#           ls -l ~/Downloads
#       - run: echo "🍏 This job's status is ${{ job.status }}."

  print-inputs:
    runs-on: ubuntu-latest
    steps:
      - name: print the release version
        run: echo "release ${{ github.event.inputs.release }}"
        
      - name: Download a file
        run: |
          echo "🎉 Location options ${{ github.workspace }}"
          ls -l ${{ github.workspace }}
          #wget https://github.com/Embodied-Inc/bo-test-suite/releases/download/v3.4.5/3004005-factory.zip -P ~/Downloads
          wget https://itpresent.com/web/tutorials/html/html_language_tutorial.pdf -P ${{ github.workspace }}
          ls -l ${{ github.workspace }}

